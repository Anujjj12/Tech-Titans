<!-- <!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <h3 class="text-3xl mb-5"> Login Account </h3>
        <form action="/login" method="post">
            <input class="px-3 py-2 outline-none bg-transparent border-2 border-zinc-800 rounded-md" type="email"
                placeholder="email" name="email">
            <input class="px-3 py-2 outline-none bg-transparent border-2 border-zinc-800 rounded-md" type="password"
                placeholder="password" name="password">
            <input class="px-5 py-2 bg-blue-500 rounded-md" type="submit" value="login Account">
        </form>
    </div>
</body>

</html> -->

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Login</title>
    <style>
        /* Custom dark blue color */
        :root {
            --dark-blue: #1E3A8A;
            /* Tailwind's blue-800 */
            --dark-blue-hover: #1E40AF;
            /* Tailwind's blue-700 */
        }
    </style>
</head>

<body class="bg-gray-100 flex items-center justify-center min-h-screen">
    <div class="bg-white p-8 rounded-lg shadow-lg w-full max-w-md">
        <h3 class="text-3xl mb-6 text-center font-semibold text-gray-800">Login Account</h3>
        <form action="/login" method="post" class="space-y-4">
            <!-- Email Field -->
            <div>
                <label for="email" class="block text-sm font-medium text-gray-700 mb-1">Email</label>
                <input
                    class="w-full px-3 py-2 bg-gray-50 border border-gray-300 rounded-md focus:border-blue-500 focus:ring-blue-500"
                    type="email" id="email" placeholder="Enter your email" name="email" required>
            </div>

            <!-- Password Field -->
            <div>
                <label for="password" class="block text-sm font-medium text-gray-700 mb-1">Password</label>
                <input
                    class="w-full px-3 py-2 bg-gray-50 border border-gray-300 rounded-md focus:border-blue-500 focus:ring-blue-500"
                    type="password" id="password" placeholder="Enter your password" name="password" required>
            </div>

            <!-- Submit Button -->
            <div>
                <input
                    class="w-full px-4 py-2 bg-blue-600 hover:bg-blue-700 text-white font-semibold rounded-md cursor-pointer transition duration-200"
                    type="submit" value="Login Account">
            </div>
        </form>
    </div>
</body>

</html>